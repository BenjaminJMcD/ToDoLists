export default class Item {
    constructor(list, name, priority, notes, price, sets, reps, weight, subFolder) {
        this.list = list;
        this.name = name;
        this.priority = priority;
        this.notes = notes;
        this.price = price;
        this.sets = sets;
        this.reps = reps;
        this.weight = weight;
        this.subFolder = subFolder;
    }
}